with "config/string_converter_dispatcher_config.gpr";
project String_Converter_Dispatcher is

   for Source_Dirs use ("src/", "config/");
   for Object_Dir use "obj/" & String_Converter_Dispatcher_Config.Build_Profile;
   for Create_Missing_Dirs use "True";
   for Exec_Dir use "bin";
   for Main use ("string_converter_dispatcher.adb");

   package Compiler is
      for Default_Switches ("Ada") use String_Converter_Dispatcher_Config.Ada_Compiler_Switches;
   end Compiler;

   package Binder is
      for Switches ("Ada") use ("-Es"); --  Symbolic traceback
   end Binder;

   package Install is
      for Artifacts (".") use ("share");
   end Install;

end String_Converter_Dispatcher;
